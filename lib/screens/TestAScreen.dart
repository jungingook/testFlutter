import 'package:flutter/material.dart';
import '../widgets/LogoBox.dart';

class FirstRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container (
      color: Colors.white,
      child: LogoBox(),
    );
  }
}